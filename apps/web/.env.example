# apps/api'nin taban URL'i (bkz. src/lib/api.ts). NEXT_PUBLIC_ öneki, Next.js'in
# bu değişkeni tarayıcıya (client bundle'a) da açması için gerekli.
NEXT_PUBLIC_API_URL="http://localhost:3001/api/v1"
